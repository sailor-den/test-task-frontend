# Тестовое задание на VueJS

Реализация тестового задания на Vue 3.

## Описание

Страница с изображениями (вещами), состоящая из 4 блоков:

- **Сверху слева**: выбранные вещи из блока снизу-слева (от 1 до 6 вещей)
- **Сверху справа**: выбранная вещь из блока снизу-справа (только 1 вещь)
- **Снизу слева**: вещи у пользователя (можно выбрать от 1 до 6)
- **Снизу справа**: вещи на выбор (можно выбрать только 1)

## Механика работы

- Снизу-справа можно выбрать одновременно **только 1 вещь**, которая отображается справа-сверху
- Снизу-слева можно выбрать **от 1 до 6 вещей**, которые отображаются в верхней левой части в порядке выбора

## Установка и запуск

```bash
# Установка зависимостей
npm install

# Запуск dev-сервера
npm run dev

# Сборка для production
npm run build

# Просмотр production сборки
npm run preview
```

## Технологии

- Vue 3 (Composition API)
- Vite
- SCSS
